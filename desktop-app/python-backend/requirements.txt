# Desktop App: Unified AI Safety Dashboard with ChatGPT Integration
# Requirements for desktop deployment (Electron + Streamlit)

# Core Dependencies
streamlit>=1.30.0
python-dotenv>=1.0.0

# Data Processing
numpy>=1.24.0
pandas>=2.0.0

# Machine Learning
scikit-learn>=1.3.0
joblib>=1.3.0

# AWS Services (for embeddings)
boto3>=1.28.0
botocore>=1.31.0

# OpenAI Integration (NEW - for ChatGPT)
openai>=1.3.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.18.0

# API & HTTP (for LLM API calls)
requests>=2.31.0

# Utilities
pyyaml>=6.0
pillow>=10.0.0

# Installation:
# pip install -r requirements.txt
#
# Note: This app integrates all 3 stages:
# - Requires shared modules from ../shared/
# - Requires App 1, 2, 3 components
# - Requires PCA models in ../models/
# - Optionally requires API keys for real LLM calls:
#   - OPENAI_API_KEY (for GPT-3.5, GPT-4)
#   - ANTHROPIC_API_KEY (for Claude)
#   - MISTRAL_API_KEY (for Mistral)
# - Can use AWS Lambda endpoints (no API keys needed)
# - Can use mock client (no API keys needed)
