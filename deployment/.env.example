# =============================================================================
# Vector Precognition Deployment Suite - Environment Configuration
# =============================================================================
#
# IMPORTANT: This file contains sensitive credentials. Never commit to git!
#
# For detailed setup instructions, see: docs/ENVIRONMENT_SETUP_GUIDE.md
#
# =============================================================================

# -----------------------------------------------------------------------------
# AWS Bedrock Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# These credentials are required for embedding generation in all apps.
#
# How to get these:
# 1. Create AWS account at: https://aws.amazon.com
# 2. Enable AWS Bedrock access for Titan Text Embeddings
# 3. Create IAM user with programmatic access
# 4. Attach AmazonBedrockFullAccess policy
# 5. Copy Access Key ID and Secret Access Key
#
# See: docs/ENVIRONMENT_SETUP_GUIDE.md for detailed instructions

AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here
AWS_DEFAULT_REGION=us-east-1

# -----------------------------------------------------------------------------
# LLM API Endpoints (OPTIONAL - App 4 only)
# -----------------------------------------------------------------------------
# These endpoints are only needed for App 4 (Unified Dashboard) live chat.
# Apps 1, 2, and 3 work without these endpoints.
#
# Options:
# 1. Leave blank and use Mock Client (no API keys needed)
# 2. Add your AWS Lambda or custom API endpoints
# 3. Modify api_client.py to use OpenAI/Anthropic/Mistral APIs directly
#
# Format: HTTPS POST endpoint accepting: {"message": "text"}
#         Returns: {"reply": "response"}

GPT35_ENDPOINT=
GPT4_ENDPOINT=
CLAUDE_ENDPOINT=
MISTRAL_ENDPOINT=

# -----------------------------------------------------------------------------
# Application Settings (OPTIONAL)
# -----------------------------------------------------------------------------

# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Maximum file upload size in MB
MAX_FILE_SIZE_MB=10

# Session timeout for App 4 (minutes)
SESSION_TIMEOUT_MINUTES=60
